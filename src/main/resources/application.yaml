server:
  port: 8091

spring:
  application:
    name: web-redis
  jackson:
    time-zone: GMT+8
  data:
    redis:
      repositories:
        enabled: off
  redis:
    host: redis
    port: 6379
    database: 0
    client-type: lettuce

security:
  token: ${WEB_REDIS_TOKEN:dress}